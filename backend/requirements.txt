# requirements.txt
# Purpose: Python dependencies for the FastAPI backend.
# Versions are pinned for reproducibility. Install with: pip install -r requirements.txt
#
# ── Web framework ──────────────────────────────────────────────────────────
fastapi>=0.115.0          # Async web framework; auto-generates OpenAPI docs
uvicorn[standard]>=0.32.0 # ASGI server that runs FastAPI

# ── Database ───────────────────────────────────────────────────────────────
sqlalchemy>=2.0.36        # ORM — maps Python classes to PostgreSQL tables
asyncpg>=0.30.0           # Async PostgreSQL driver (used with SQLAlchemy async)
alembic>=1.14.0           # Database migrations (schema version control)

# ── Authentication ─────────────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0  # JWT encode/decode
passlib[bcrypt]>=1.7.4            # Password hashing (bcrypt)
python-multipart>=0.0.12          # Required for form data (login forms)

# ── Configuration ──────────────────────────────────────────────────────────
pydantic-settings>=2.6.0  # Load settings from .env files
python-dotenv>=1.0.1      # Read .env files

# ── HTTP client (for LLM APIs) ───────────────────────────────────────────────
httpx>=0.28.0             # Async HTTP client for OpenAI-compatible APIs

# ── Development / testing (optional) ────────────────────────────────────────
pytest>=8.3.0
pytest-asyncio>=0.24.0
